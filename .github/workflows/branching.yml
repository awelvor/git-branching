---
name: tuto
"on": push
jobs:
  check_tuto:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - run: yamllint .github/workflows/branching.yml
      - run: bash branchig.sh
      - run: echo "end of file"
